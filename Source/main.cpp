#include "CM.h"

int main(){
    //追加機能用
    /*while(true){
    int choice;
    choice = selection();*/
    //Sourceフォルダへの絶対パスを下記のworkingDirectoryに入力してください。
    /*string workingDirectory = ""
    fs::current_path(workingDirectory);*/
    listProperties();
    charaBuild();
    return 0;
}
